
const initialStyle = {
  width: '1200px',
  height: '800px',
  background: '#efefFF'
};

export default function style(state = initialStyle, action) {

  switch (action.type) {
    default:
      return state;
  }

}
